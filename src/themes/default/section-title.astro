---
const { title, subTitle, excerpt, heroImage, logo, variant, clazz } =
  Astro.props;

let className = variant === "center" ? " text-center lg:text-center " : "";
---

{
  title && (
    <section class={`max-w-7xl mx-auto py-0  ${className} ${clazz || ""}`}>
      {subTitle && (
        <p class="mt-2 uppercase text-sm leading-normal font-bold  text-accent-600">
          {subTitle}
        </p>
      )}

      {title && (
        <p class="mt-4 text-4xl font-semibold tracking-tight text-pretty  sm:text-5xl">
          {title}
        </p>
      )}

      {excerpt && <p class="max-w-7xl py-2  mx-auto ">{excerpt}</p>}
    </section>
  )
}

<!-- || variant === 'grid' --><!-- {JSON.stringify(variant)} --><!-- <div class={`max-w-7xl mx-auto py-5 pt-10 ${className}`}>
  <p class="mt-2 text-sm uppercase font-normal" transition:name={subTitle}>
    {subTitle}
  </p>
  <h1
    class="mt-4 text-3xl text-balance font-normal mx-auto"
    transition:name={title}
  >
    {title}
  </h1>
  <p
    class="mt-4 text-base text-gray-500 max-w-3xl mx-auto"
    transition:name={excerpt}
  >
    {excerpt}
  </p>
</div> -->
