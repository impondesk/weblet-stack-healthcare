---
import SectionTitle from "../section-title.astro";

const { title, imageList } = Astro.props;
---

<section class="py-12 sm:py-16 lg:py-20">
  <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
    <div class="lg:flex lg:items-end lg:justify-between">
      <div class="max-w-md-x mx-auto text-center lg:text-left lg:mx-0">
        <SectionTitle {...Astro.props} />
      </div>

      <!-- <div class="hidden lg:items-center lg:justify-end lg:space-x-3 lg:flex">
        <button
          type="button"
          class="p-1.5 -m-1.5 text-gray-300 transition-all duration-200 rounded-full hover:text-gray-600 hover:bg-gray-100"
        >
          <svg
            class="w-6 h-6"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            stroke-width="2"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
          </svg>
        </button>

        <button
          type="button"
          class="p-1.5 -m-1.5 text-gray-300 transition-all duration-200 rounded-full hover:text-gray-600 hover:bg-gray-100"
        >
          <svg
            class="w-6 h-6"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            stroke-width="2"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </button>
      </div> -->
    </div>
  </div>

  <div
    id="gallery-scroll"
    class="flex w-full gap-6 pb-8 mt-12 overflow-x-auto sm:mt-16 snap-x hide-scrollbar"
  >
    {
      imageList &&
        imageList.length > 0 &&
        imageList.map((instance) => (
          <div class="relative snap-start scroll-ml-6 shrink-0 first:pl-6 last:pr-6">
            <div class="w-[332px] h-[auto] min-h-[500px] max-h-[500px] bg-gray-200 rounded-lg overflow-hidden flex items-center justify-center">
              <img
                class="w-full h-full object-cover"
                src={instance?.image?.url}
                alt={instance?.image?.alt || instance?.image?.caption || title}
              />
            </div>
            {instance?.caption && (
              <p class="mt-5 text-base font-bold text-gray-600">
                {instance?.caption}
              </p>
            )}
          </div>
        ))
    }
  </div>
</section>
