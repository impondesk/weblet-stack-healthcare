---
import SectionTitle from "../../section-title.astro";
import PostMasonryItem from "./post-masonry-item.astro";

const { records } = Astro.props;
---

<!-- {JSON.stringify(records, null, 2)} -->
<section class="relative w-full max-w-7xl mx-auto lg:pb-24">
  <SectionTitle {...Astro.props} />

  <div class="py-4 px-2 lg:px-0">
    <ol
      class="columns-1 sm:columns-2 lg:columns-4 gap-4 [column-fill:_balance]"
      role="list"
    >
      {records?.map((post) => <PostMasonryItem {...post} />)}
    </ol>
  </div>
</section>
